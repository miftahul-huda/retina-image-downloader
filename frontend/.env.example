# Optional: API URL override (if different from served location)
# VITE_API_URL=http://localhost:3000/api

# Google OAuth Client ID (required for login)
VITE_GOOGLE_CLIENT_ID=your-google-client-id-here
